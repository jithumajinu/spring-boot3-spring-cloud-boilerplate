package com.openapi.cloud.core;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class AuthServiceApplicationTests {

	@Test
	void contextLoads() { // NOSONAR
		/*
		 * This test method is intentionally empty. Its purpose is to verify that
		 * the Spring application context loads successfully without any errors.
		 * If the context fails to load, the test will fail automatically.
		 */
	}

}
