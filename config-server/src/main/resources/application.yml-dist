server:
  port: 8888 
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/jithumajinu/config-server-spring-boot
          search-paths: config-repo
          basedir: ./repo
